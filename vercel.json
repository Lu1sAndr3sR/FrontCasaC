{
  "buildCommand": "npx ng build --configuration=production",
  "outputDirectory": "dist/casa-c/browser",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
